import React from 'react';
import {StyleSheet, View} from 'react-native';

interface Props {}

export default function PublicUploadsTab({}: Props) {
  return <View style={styles.container}></View>;
}

const styles = StyleSheet.create({
  container: {},
});
